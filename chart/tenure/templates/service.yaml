apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenure.fullname" . }}-mongo
spec:
  selector:
    app.kubernetes.io/component: mongo
    {{- include "tenure.selectorLabels" . | nindent 4 }}
  ports:
    - port: 27017
      targetPort: 27017
{{- end }}