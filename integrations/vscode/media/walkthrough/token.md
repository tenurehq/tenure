## Connect to Tenure

Tenure is running locally at `http://localhost:5757`.
Paste your API token to connect this extension.

Your token was printed to the terminal when Tenure first started
and is saved at `~/.tenure/token`.
